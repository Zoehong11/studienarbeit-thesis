\chapter{Methodology}
\label{chap:method}

Approaches to solving Connections fall into two broad groups. The first prompts a language model to produce the four groups directly, which is the primary solution used in prior work \autoref{sec:prior-work-connections}. The second scores candidate groups and then searches for the partition whose groups score highest using scoring function and searching algorithm, which is served mainly as a baseline in the prior work. The hypothesis space articulated in \autoref{sec:hypothesis-space} applies to both. Most of the information needed to identify the correct partition has to come from the solving mechanism rather than from the game feedback. The two approaches differ in where that information sits. In the first it is held in the model's parameters and reached through the prompt. In the second it is held in the scoring function and the search algorithm, which solves Connections by searching for the partition with the highest score subject to the grouping constraint (described in \autoref{sec:connections-csp}).

This thesis concentrates on the second approach. A scoring function can be taken apart into components that are swapped and evaluated one at a time, so the contribution of each component to the final result can be measured directly. A language model is however a black box. A change in the prompt can change the output, but it is hard to directly observe and explain what exactly happened.
% This does not rule language models out. They can be a component in the scoring function, and \autoref{subsec:...} examines whether they do so well.

The scoring function assigns a score to every partition, and works generally in two steps. A similarity is first computed for every pair of the sixteen words. These similarities are then assembled into scores for candidate groups, and from those into scores for partitions. Under the one-shot setting the partition with the highest score is returned as the answer. Under the interactive setting the scores are instead treated as a prior over all partitions and revised as responses come in.

\autoref{sec:scoring-functions} describes the scoring function and the choices it involves. \autoref{sec:one-shot-solving} describes solving under the one-shot setting. \autoref{sec:interactive-solving} describes what the interactive setting adds. Lastly, \autoref{sec:pipeline} assembles the components into a complete system.

\section{Scoring Functions}
\label{sec:scoring-functions}

The scoring function assigns a value to every partition, so that the best partition can be identified. It first produces a similarity for every pair of words, collected in a $16 \times 16$ matrix of $\binom{16}{2} = 120$ pairwise similarities. These similarities are then assembled into scores for groups and for partitions.

A candidate group is any four of the sixteen words, and each contains $\binom{4}{2} = 6$ word pairs. Its score is the sum of all the pairwise similarities in the group:
\begin{equation}
s(g) = \sum_{\{i,j\} \subset g} \text{sim}[i,j].
\label{eq:group-score}
\end{equation}

A partition is then scored by adding the scores of its four groups, which is the quantity maximized in \autoref{eq:weighted-csp}. Since the same group appears in many partitions, all $\binom{16}{4} = 1820$ group scores are computed once and stored, and partition scores are assembled from them by lookup.

What varies is how $\text{sim}[i,j]$ is obtained, and \autoref{fig:scoring-function} shows the two configurations compared. The first embeds each word as it is, so that a word has one vector and the similarity between two words is the cosine similarity of those vectors. The second expands each word into a set of texts, one per meaning, and embeds each text separately, so that a word is represented by a pool of vectors. The similarity between two words is then obtained by aggregating over the most similar pairings of their meanings, as detailed in \autoref{subsec:semantic-enrichment}.
\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{img/connections-score-function}
    \caption{The two configurations of the scoring function compared in this thesis. Dashed borders mark the points where configurations differ; solid borders mark stages that are the same throughout. Configuration (1) embeds each word directly. Configuration (2) expands each word into a pool of meanings first, and then aggregates over the most similar pairings of meanings into the single similarity.}
    \label{fig:scoring-function}
\end{figure}

\subsection{Semantic Enrichment}
\label{subsec:semantic-enrichment}

A word can carry multiple meanings. The word "bank" can refer to a financial institution or to the edge of a river, and an embedding of the bare word blends both.

The simplest way to obtain $\text{sim}[i,j]$ is to embed each word directly and take the cosine similarity of the two vectors, which is what the embedding-based baselines in \autoref{sec:prior-work-connections} do, and those baselines generally perform poorly. Two kinds of embedding model are involved. Static word embeddings, such as Word2vec and GloVe, assign one fixed vector per word, so every meaning of a polysemous word is compressed into a single representation, a property known as the meaning conflation deficiency. Contextualised models, such as BERT, address this by producing a vector that depends on the surrounding text \cite{camacho-colladosWordSenseEmbeddings2018}. This thesis specifically uses sentence-embedding models, a family of contextualised models that pool an entire piece of text into a vector, making it possible to embed a word's definition as a whole rather than the bare word alone.

Connections requires the intended meaning of each word to be identified before the words can be partitioned, and the most obvious meaning is not necessarily the intended one. A sentence-embedding model is well suited to this, because the vector it produces reflects the specific text it is given, unlike a static embedding, which assigns the same vector to a word regardless of what text accompanies it. \citeauthor{toddMissedConnectionsLateral2024} likewise use a sentence-embedding model as their baseline \cite{toddMissedConnectionsLateral2024}. This property motivates enriching each word with its candidate meanings before embedding.

Our solver supplies every candidate meaning of a word directly as a set of texts, each describing one meaning. Each text is embedded separately with a sentence-embedding model, giving each of a word's meanings its own vector instead of averaging them into one. This avoids the meaning conflation described above and lets the intended meaning stand out on its own. If two words are strongly similar under one particular pairing of their meanings, that pairing is itself evidence of what the puzzle intends.

% \subsubsection{Knowledge Source}

% Therefore, the first stage of the scoring function replaces each word with a set of texts, each describing one meaning, so that the meanings can be kept apart. Three resources were examined as suppliers of those texts, each chosen to target one or more of the relation types identified in \autoref{tab:connections_relation_taxonomy}.

% WordNet organizes English vocabulary into synsets. A synset is a set of words that share one specific meaning, with a separate synset maintained for each meaning of a polysemous word \cite{millerWordNetLexicalDatabase1995}. Every synset carries a short definition, called a gloss. For a given word, each of its synsets contributes its gloss as a separate text. “Bank” has synsets for the financial-institution sense and for the riverside sense, and each gloss enters the pool on its own rather than being merged with the other. WordNet's glosses target the semantic similarity and semantic relatedness categories, where word meaning and shared theme drive the grouping.

% Wikipedia provides encyclopedic text covering proper nouns, cultural references and domain-specific topics that a lexical resource does not include. For a given word, the opening summary of the corresponding article is retrieved and added to the pool as a single text. Wikipedia targets the referential categories, which require knowledge of named entities and other real-world facts.

% Datamuse is a public API returning words related to a query along mechanically defined axes, including rhymes, phonetic near-matches, spelling similarity, and corpus co-occurrence. The words it returns are added to the pool directly, without definitions attached. Datamuse is included to target the form- or rule-based categories, where the group depends on the spelling or sound rather than meaning.

% The three resources are controlled by independent switches on the same routine, so a configuration is specified by which of them are enabled. Every other stage of the scoring function is held fixed, so any difference between configurations can be attributed to the choice of resources alone.

% Where none of the enabled resources returns anything for a word, the bare word itself is used as the single entry in its pool. This rule applies to the combined output of whichever resources are enabled rather than to each of them separately, so a word falls back to its surface form only if every enabled resource fails to cover it.

The texts themselves come from external lexical and encyclopedic resources. Which resources are used is a configuration choice rather than part of the mechanism. The knowledge resources examined in this thesis and the effect of each are reported in \autoref{chap:experiment}. When none of the enabled resources returns anything for a word, the bare word itself is used as the single entry in its pool, so that every word is represented.

After enrichment, each word is represented by a pool of vectors rather than a single one. Comparing two words therefore produces not one similarity but many, one for every pairing of a meaning from the first word with a meaning from the second. A single value, $\text{sim}[i,j]$, still has to be produced from this collection.

Both extremes raise a concern. Averaging over every pairing treats all of a word's meanings as equally relevant, even though a puzzle intends only one, which risks diluting the one match that actually matters. A word with many meanings would be diluted more than a word with few, simply because it contributes more irrelevant pairings to the average. Taking only the single highest similarity avoids this risk, but relies on one pairing alone, so a single coincidentally high similarity could decide the result with nothing to cushion it.

This thesis takes the average similarity over the top-$k$ highest-similarity pairings. Small $k$ behaves like taking the maximum alone. Large $k$ approaches averaging over everything. $k$ is a parameter that interpolates between the two extremes, not a value that can be fixed in advance.

The value of $k$ is determined experimentally rather than fixed here. A range of values is compared, and the one that performs best is adopted, as reported in \autoref{chap:experiments}.

\section{Solving under the One-Shot Setting}
\label{sec:one-shot-solving}

Under the one-shot setting, the solver submits a single partition without receiving any feedback, so the task is to identify that one partition directly. With all $\binom{16}{4} = 1820$ candidate groups scored by \autoref{eq:group-score}, what remains is to solve \autoref{eq:weighted-csp} directly, searching the $|\mathcal{H}| = 2{,}627{,}625$ valid partitions (\autoref{sec:hypothesis-space}) for the one with the highest total score. The highest score can be pursued in two different ways. A local approach picks the highest-scoring group at each step, then repeats the process on the words that remain until a partition is formed. A global approach instead searches for the partition whose four groups have the highest score when summed together, even if no single group in it is the highest-scoring one on its own. Two search algorithms, one for each approach, are compared in this thesis, and \autoref{fig:one-shot-pipeline} gives an overview.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.6\linewidth]{img/connections-one-shot}
    \caption{Pipeline for solving Connections under the one-shot setting.}
    \label{fig:one-shot-pipeline}
\end{figure}

The first is greedy. The highest-scoring group among all 1820 candidates is fixed as the first group. Its four words are removed, and the highest-scoring group among the candidates formed from the remaining twelve is fixed as the second. The same is done for the remaining eight words, and the last four words form the fourth group by default. Each choice is made from what scores best at that point and is never revisited.

The greedy procedure never reconsiders an earlier choice, even if it turns out to leave the remaining words in a bad position. The second algorithm instead achieves the global approach directly, using dynamic programming as the method that makes this possible.

Dynamic programming solves a problem by considering every possible subproblem and reusing each one's solution wherever it reappears, rather than committing to a single path through it \cite{DynamicProgramming2026}. Considering every subproblem this way is what guarantees the best score found is the global best, matching the global approach described above, while reusing solutions instead of recomputing them is what keeps this exhaustive search efficient. Connections can be solved with dynamic programming. Since a partition's score is the sum of its four group scores, the best partition of a set of words is simply the best last group plus the best partition of the words left over, so solving the full puzzle reduces to solving the same kind of problem on smaller sets of words. Because many different choices of groups lead back to the same smaller set, solving each subset once is enough, and the small number of reachable subsets keeps this cheap enough to run on every puzzle.

Recall that $W$ denotes the sixteen puzzle words. Let $M$ denote a subset of these words. In practice, $M$ is represented as a 16-bit mask, an integer whose $i$-th bit is $1$ if the $i$-th word is included in $M$ and $0$ otherwise, so that every subset of $W$ corresponds to exactly one such integer. This representation is used in this thesis's implementation because it makes $\text{best}(M)$ cheap to store and look up for every subset, since $M$ itself can serve directly as an index, and because operations such as removing a group from $M$ reduce to fast bitwise arithmetic rather than manipulating an explicit list of words. Let $\emptyset$ denote the empty subset containing no words at all, corresponding to the mask with every bit set to $0$. For any such $M \subseteq W$, let $\text{best}(M)$ denote the highest score obtainable by partitioning $M$ alone into groups of four. When $M$ is empty there are no words left to group, so nothing is scored, and $\text{best}(\emptyset) = 0$. This is the starting point the rest of the computation builds on.

For a non-empty $M$, imagine one specific group $g$ is part of the eventual solution. The remaining words, $M \setminus g$, still need to be partitioned as well as possible. The best achievable score for them is by definition $\text{best}(M \setminus g)$. Committing to $g$ therefore contributes a total of $s(g) + \text{best}(M \setminus g)$. It is not known in advance which group actually belongs to the best solution. Every possible $g$ is therefore tried, and the highest resulting total is kept. This defines $\text{best}(M)$ recursively, in terms of $\text{best}$ evaluated on strictly smaller subsets. It follows the same subset-enumeration technique used for exact dynamic programming over subsets \cite{heldkarpDynamicProgrammingApproach1962},
\begin{equation}
\text{best}(M) = \max_{\substack{g \subseteq M \\ |g| = 4}} \Big( s(g) + \text{best}(M \setminus g) \Big).
\label{eq:dp-recurrence}
\end{equation}
This recurrence does not run in circles. Each step removes a group of four words, so $M \setminus g$ is always four words smaller than $M$. Every subproblem therefore eventually reduces to the base case $\text{best}(\emptyset) = 0$. This thesis evaluates \autoref{eq:dp-recurrence} bottom-up. Masks are processed in order of increasing size, starting from $\text{best}(\emptyset) = 0$. By the time $\text{best}(M)$ is computed for any given $M$, every smaller $\text{best}(M \setminus g)$ it needs has already been computed and stored. No value is therefore ever recomputed. The last mask processed is $W$ itself, giving $\text{best}(W)$, the score of $P^*$ in \autoref{eq:weighted-csp}.

The score alone is not the answer. To recover the four groups that make up $P^*$, whichever group $g$ achieves the maximum in \autoref{eq:dp-recurrence} is recorded for every $M$ along the way. Starting from $W$, this recorded group is read off, removed, and the same lookup is repeated on what remains, until all sixteen words have been assigned.

The two differ in what they take into account. The greedy procedure judges a group by its own score alone. The recursion judges it by $s(g) + \text{best}(M \setminus g)$, so a group is only chosen if what remains after removing its words can still be partitioned well. A group that scores highly on its own can therefore be rejected, and a group scoring less can be preferred, if the rest of the board resolves better that way. Whether this changes the outcome in practice is reported in \autoref{chap:experiment}.

% \section{Solving under the Interactive Setting}
% \label{sec:interactive-solving}

% TODO: content to be written. Outline below, structure only for now.

% Under the interactive setting, a single best partition is not enough. The scores
% of ALL valid partitions are needed, because a distribution over partitions has
% to be maintained and updated as feedback arrives after each submission. This is
% why dynamic programming does not apply here: DP only ever recovers the single
% best-scoring partition, not the scores of every partition. Full enumeration with
% a lookup table is used instead.

% \subsection{From Scores to Probabilities}
% - full enumeration of all valid partitions, scores stored in a lookup table
% - softmax converts partition scores into a probability distribution
% - temperature T controls how peaked/flat the resulting distribution is
% - the problem with T (needs elaboration)

% \subsection{Updating Beliefs from Feedback}
% - Bayesian update: condition the distribution on the game's response after each
%   guess (correct / one-away / wrong)
% - the next group submitted is the one with the highest marginal probability
%   under the (still uncertain) distribution



% \section{Solving under the Interactive Setting}
% \label{sec:interactive-solving}

% Under the one-shot setting, the highest-scoring partition is the answer, but under the interactive setting it needs to adapt. A response can rule out the partition currently believed to be best, and the solver then has to fall back on what else was possible.

% The approach taken for the interactive setting follows the same idea that \autoref{sec:strategies} describes for solving Wordle, where a candidate set of possible solutions is narrowed down as feedback arrives. This thesis applies the same idea to the candidate set of possible partitions. Connections has 2,627,625 partitions and each submission returning a response that rules some of them out. What is borrowed is the elimination mechanism, not the valuations discussed in the pior work.
% % What is borrowed is the elimination mechanism, not the valuations described in the pior work. Those valuations count candidates: how many remain in the worst case, how many distinct responses a guess splits them into, how much the count is expected to shrink. Counting is the right thing to do when every candidate is equally likely, which is the situation in Wordle and Mastermind, where nothing distinguishes one secret from another before the first guess. Connections is not in that situation. The scoring function ranks the partitions before anything is submitted, and \autoref{chap:problem-formulation} showed that this ranking needs to carry most of the information available.
% Becasue a candidate set treats every surviving candidate alike, whereas the scoring function of \autoref{sec:scoring-functions} already ranks the partitions before anything is submitted. Moreover, \autoref{chap:problem-formulation} showed that the scoring function is where most of the information has to come from. 

% Thus, the ranking is kept by assigning each partition a numeric belief, a continuous measure of how likely it is to be correct. This belief is then revised as responses come in.

% A belief for every partition requires the score of every partition, not just the highest one.
% The recurrence in \autoref{eq:dp-recurrence} does not provide this. At each step it keeps only the best score reached so far and discards every other choice along the way. It recovers a single best partition, not the score of each one.
% Under the interactive setting, every partition's score is instead computed directly. Each partition consists of four groups, and each group already has a score stored from \autoref{eq:group-score}. A partition's score is simply the sum of its four group scores. Looking up four numbers and adding them is done for all 2,627,625 partitions at once, without recursion or caching. These are exactly the scores the beliefs are computed from.

% Scores are not probabilities, and a distribution over partitions is what the solver has to maintain. The scores are converted using a softmax,
% \begin{equation}
% P_0(h) = \frac{\exp\big(\text{score}(h)/T\big)}{\sum_{h'} \exp\big(\text{score}(h')/T\big)},
% \label{eq:softmax-prior}
% \end{equation}
% where $h$ ranges over all partitions and $T$ controls how concentrated the result is. A large $T$ flattens the differences between scores and spreads the probability evenly; a small $T$ concentrates it on the highest-scoring partitions. The default choice $T = 1$ is not usable here: it gives a prior with an entropy of 21.2 bits against the maximum of 21.32 from \autoref{eq:prior-entropy}, meaning that the distribution is almost uniform and nearly all of the ordering produced by the scoring function is lost in the conversion. $T$ is therefore set separately, and how is described in \autoref{chap:experiments}.

% Once a group has been submitted and a response received, the belief is revised. For any partition and any submitted group, the response that would follow is fully determined by the rules in \autoref{sec:problem-setting}, so it can be computed for every partition still under consideration. Those whose predicted response differs from the one actually received are ruled out, and the probability mass is renormalised over the rest. No approximation is involved, since every partition can be checked individually.

% A response of \emph{correct} is handled in the same way rather than as a special case. The set of partitions is never rebuilt: it remains the original 2,627,625, indexed by the original sixteen words, throughout the game. What a \emph{correct} response rules out is every partition that does not contain the confirmed group, and the belief is renormalised over those that do. Only the set of candidate groups shrinks, since submissions are drawn from the words not yet resolved. This keeps one property intact for the whole game: the true partition is consistent with every response received, so it is never ruled out, and the belief always retains it.

% What remains is choosing which group to submit. Every group of four still-unresolved words is a candidate, and each is scored by its marginal probability: the total belief held by those surviving partitions that contain this exact group as one of their four. The candidate with the highest marginal is submitted. This is not the same as taking the most probable partition and submitting one of its groups. A group can hold the largest share of belief across many partitions without appearing in the single most probable one, and the two criteria can therefore disagree.


\section{Solving under the Interactive Setting}
\label{sec:interactive-solving}
The interactive setting allows only one group to be submitted at a time, so the solver must select the group it currently believes in the most. This choice cannot be made from a group's own score alone. As established in \autoref{sec:one-shot-solving}, a group should be picked when the remaining words can still form valid groups, so belief has to attach to whole partitions rather than to individual groups. \autoref{fig:interactive-pipeline} gives an overview of the resulting loop.

\begin{figure}[t]
    \centering
    \includegraphics[height=0.7\textheight,width=0.4\linewidth,keepaspectratio]{img/connections-interactive}
    \caption{Pipeline for solving Connections under the interactive setting.}
    \label{fig:interactive-pipeline}
\end{figure}

A probability for every partition requires the score of every partition, not just the highest one. The recurrence used in \autoref{sec:one-shot-solving} does not provide this. At each step it keeps only the best score reached so far and discards every other choice along the way. Under the interactive setting, every partition's score is instead computed directly. A partition's score is the sum of its four group scores, looked up and added for all 2,627,625 partitions at once.

% \subsection{Representing Partitions and Groups}
% Each group of four words is represented as a 16-bit mask, the same representation
% used for the subsets in \autoref{sec:one-shot-solving}. Bit $i$ is set to $1$ if the
% $i$-th word belongs to the group. A complete partition is four such masks, one for
% each of its groups.
%
% Every one of the 2,627,625 valid partitions is enumerated once, at the start of the
% program, and stored as an array of four masks per partition. This structure depends
% only on how sixteen positions can be split into four disjoint groups of four, not on
% the words or the puzzle, so it is computed once and reused across every puzzle
% solved. What changes from puzzle to puzzle is only the score, and later the
% probability, attached to each partition.
%
% The role masks play here differs from their role in \autoref{sec:one-shot-solving}.
% There, a mask represents how many words remain to be assigned, and it shrinks from
% sixteen bits down to zero as the recursion in \autoref{eq:dp-recurrence} proceeds.
% Here, there is no such recursion. Every partition is enumerated directly, and a mask
% instead serves as an index into lookup tables and as an operand for bitwise
% comparisons. The representation is the same, but what it is used for is not.

Thus, in this thesis, a group's belief is obtained by summing the probability of every partition that contains it. In order to do so, the solver needs to hold the probability for every partition. The probability distribution is transformed from the score of each partition by using softmax.

Let $h$ range over the $|\mathcal{H}| = 2{,}627{,}625$ partitions established in \autoref{sec:hypothesis-space}. Before any feedback has been received, the probability assigned to partition $h$ is:
\begin{equation}
P_0(h) = \frac{\exp\big(\text{score}(h)/T\big)}{\sum_{h'} \exp\big(\text{score}(h')/T\big)}.
\label{eq:softmax-prior}
\end{equation}
The subscript $0$ marks this as the belief held before any response has been observed.
% Exponentiating each partition's score in the numerator makes every value positive. Dividing by the sum of these values over every partition, in the denominator, makes the results add up to one. Together, these two steps turn the raw scores into a valid probability distribution.
The choice of temperature $T$ affects the resulting distribution. The specific value used is determined experimentally and is discussed in \autoref{chap:experiment}.

% Each candidate group's marginal probability, the quantity introduced above, is obtained by grouping the partition probabilities by the masks of the groups they contain and summing within each group. This bucketed summation is done once, in a single pass over all 2,627,625 partitions, producing a lookup table indexed by group mask. Looking up a candidate's marginal probability afterward costs nothing beyond a single table access. Only groups formed from words that remain unresolved are valid candidates, since every already-confirmed group has already been submitted. The group submitted each round is the one maximizing this marginal probability among candidates formed from the remaining words, not among all $\binom{16}{4}$ groups.

Each candidate group's marginal probability is obtained by summing the probabilities of every partition that contains it. Only groups formed from words that remain unresolved are valid candidates. The group submitted each round is the one with the highest marginal probability among these candidates.

Once a group has been submitted and a response received, these probabilities are revised. The partition that contradict from the response are removed from the candidates. After that, the probabilities of the remaining partitions no longer add up to one. A normalization step is carried out, the possibilities of the remaining are scaled up by the same factor until they add up to one. This is an exact Bayesian update, made simple by the fact that responses are deterministic. From the remaining candidates, the new submission will be chosen.

% Each response also changes what the solver should do next. A submission is chosen from the current probabilities, the response revises them, and the next submission is chosen from the revised ones. The solver makes a sequence of decisions under uncertainty, each informed by everything observed so far, rather than a single decision made once.